name: homeflow_ui
description: Shared HomeFlow design system for the Flutter apps — theme builder and formatters.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
